{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --sort --concurrency=1 --stream",
    "test": "lerna run test --concurrency=1 --stream"
  },
  "devDependencies": {
    "@types/jest": "^24.0.17",
    "jest": "^24.9.0",
    "lerna": "^3.16.4"
  },
  "dependencies": {
    "ilp-packet": "^3.0.8",
    "typescript": "^3.5.3"
  }
}
